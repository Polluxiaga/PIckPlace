# Remote Service

Optional deployment and remote-control entrypoints live here.

- `serve_policy.py`: starts a WebSocket policy server.
- `docker/`: Dockerfile and compose helpers for running the policy server in a container.

The local training/evaluation/visualization workflow does not require these files.
